type Query {
    obtenerUsuarios: [Usuario]
}

type Mutation {
    crearUsuario(nombre: String!, email: String!): Usuario
    eliminarUsuario(id: ID!): Boolean
}

type Usuario {
    id: ID
    nombre: String
    email: String
}
